- Evaluate what to write this in:
  - Exporting JS: TS, PureScript or ClojureScript.
  - Exporting to WASM: Rust?
- Rewrite tests to test the JS interface rather than the Elm one, since we need to have tests for when this is used just as a JS module.
- Set up LSP/Emacs https://www.mortens.dev/blog/emacs-and-the-language-server-protocol/
